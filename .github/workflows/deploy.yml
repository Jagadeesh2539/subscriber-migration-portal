name: 🚀 Production Deployment - Enterprise Grade

on:
  push:
    branches: [ main ]
  workflow_dispatch:

env:
  AWS_DEFAULT_REGION: us-east-1
  STACK_NAME: subscriber-migration-portal-prod
  NODE_VERSION: '18'
  PYTHON_VERSION: '3.11'
  CLEANUP_TIMEOUT_MINUTES: 15
  RDS_DELETE_TIMEOUT_MINUTES: 12
  STACK_DELETE_TIMEOUT_MINUTES: 15
  MAX_RETRY_ATTEMPTS: 5

permissions:
  contents: read
  id-token: write

jobs:
  validate:
    name: 🔍 Validate Architecture
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/setup-python@v4
        with:
          python-version: ${{ env.PYTHON_VERSION }}
      
      - name: Install validation tools
        run: |
          set -euo pipefail
          pip install --upgrade pip
          pip install aws-sam-cli boto3 jq
      
      - name: Validate SAM Template
        run: |
          set -euo pipefail
          cd aws
          
          echo "🔍 Validating SAM template syntax..."
          if sam validate --template template.yaml --region ${{ env.AWS_DEFAULT_REGION }}; then
            echo "✅ SAM template is valid"
          else
            echo "❌ SAM template validation failed"
            exit 1
          fi
      
      - name: Validate Step Functions Definitions
        run: |
          set -euo pipefail
          
          if [ ! -d "aws/stepfunctions" ]; then
            echo "⚠️ No Step Functions directory found, skipping validation"
            exit 0
          fi
          
          echo "🔍 Validating Step Functions definitions..."
          
          for file in aws/stepfunctions/*.json; do
            if [[ -f "$file" ]]; then
              filename=$(basename "$file")
              echo "  Validating: $filename"
              
              if ! jq empty "$file" 2>/dev/null; then
                echo "  ❌ Invalid JSON in $file"
                exit 1
              fi
              
              if ! jq -e '.StartAt' "$file" >/dev/null 2>&1; then
                echo "  ❌ Missing StartAt in $file"
                exit 1
              fi
              
              if ! jq -e '.States' "$file" >/dev/null 2>&1; then
                echo "  ❌ Missing States in $file"
                exit 1
              fi
              
              echo "  ✅ Valid: $filename"
            fi
          done
          
          echo "✅ All validations passed"

  discover-infrastructure:
    name: 🔍 Discover VPC Infrastructure
    runs-on: ubuntu-latest
    needs: validate
    outputs:
      vpc-id: ${{ steps.discover.outputs.vpc_id }}
      private-subnet-1: ${{ steps.discover.outputs.private_subnet_1 }}
      private-subnet-2: ${{ steps.discover.outputs.private_subnet_2 }}
      public-subnet-1: ${{ steps.discover.outputs.public_subnet_1 }}
      public-subnet-2: ${{ steps.discover.outputs.public_subnet_2 }}
    steps:
      - uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_DEFAULT_REGION }}
      
      - name: 🔍 Auto-discover VPC and Subnets with validation
        id: discover
        run: |
          set -euo pipefail
          
          echo "🔍 Discovering VPC infrastructure in ${{ env.AWS_DEFAULT_REGION }}..."
          
          VPC_ID=$(aws ec2 describe-vpcs \
            --filters "Name=is-default,Values=true" \
            --query 'Vpcs[0].VpcId' \
            --output text 2>/dev/null || echo "None")
          
          if [[ "$VPC_ID" == "None" || -z "$VPC_ID" ]]; then
            echo "⚠️ No default VPC found, searching for any available VPC..."
            VPC_ID=$(aws ec2 describe-vpcs \
              --query 'Vpcs[0].VpcId' \
              --output text 2>/dev/null || echo "None")
          fi
          
          if [[ "$VPC_ID" == "None" || -z "$VPC_ID" ]]; then
            echo "❌ CRITICAL: No VPC found in region ${{ env.AWS_DEFAULT_REGION }}"
            echo "Please create a VPC or use a different region"
            exit 1
          fi
          
          echo "✅ Found VPC: $VPC_ID"
          
          VPC_STATE=$(aws ec2 describe-vpcs \
            --vpc-ids "$VPC_ID" \
            --query 'Vpcs[0].State' \
            --output text 2>/dev/null || echo "unknown")
          
          if [[ "$VPC_STATE" != "available" ]]; then
            echo "❌ VPC is not in available state: $VPC_STATE"
            exit 1
          fi
          
          echo "🔍 Discovering private subnets..."
          PRIVATE_SUBNETS=($(aws ec2 describe-subnets \
            --filters "Name=vpc-id,Values=$VPC_ID" "Name=map-public-ip-on-launch,Values=false" \
            --query 'Subnets[?State==`available`].SubnetId' \
            --output text || echo ""))
          
          PRIVATE_1=${PRIVATE_SUBNETS[0]:-""}
          PRIVATE_2=${PRIVATE_SUBNETS[1]:-""}
          
          echo "🔍 Discovering public subnets..."
          PUBLIC_SUBNETS=($(aws ec2 describe-subnets \
            --filters "Name=vpc-id,Values=$VPC_ID" "Name=map-public-ip-on-launch,Values=true" \
            --query 'Subnets[?State==`available`].SubnetId' \
            --output text || echo ""))
          
          PUBLIC_1=${PUBLIC_SUBNETS[0]:-""}
          PUBLIC_2=${PUBLIC_SUBNETS[1]:-""}
          
          if [[ -z "$PRIVATE_1" || -z "$PRIVATE_2" ]]; then
            echo "❌ CRITICAL: Need at least 2 private subnets in VPC $VPC_ID"
            echo "Found private subnets: ${PRIVATE_SUBNETS[@]:-none}"
            echo ""
            echo "Available subnets in VPC:"
            aws ec2 describe-subnets \
              --filters "Name=vpc-id,Values=$VPC_ID" \
              --query 'Subnets[].[SubnetId,AvailabilityZone,MapPublicIpOnLaunch,State]' \
              --output table
            exit 1
          fi
          
          AZ_1=$(aws ec2 describe-subnets --subnet-ids "$PRIVATE_1" --query 'Subnets[0].AvailabilityZone' --output text)
          AZ_2=$(aws ec2 describe-subnets --subnet-ids "$PRIVATE_2" --query 'Subnets[0].AvailabilityZone' --output text)
          
          if [[ "$AZ_1" == "$AZ_2" ]]; then
            echo "⚠️ WARNING: Both private subnets are in the same AZ ($AZ_1)"
            echo "This may impact high availability"
          fi
          
          echo ""
          echo "✅ Infrastructure discovered and validated:"
          echo "  VPC:              $VPC_ID (state: $VPC_STATE)"
          echo "  Private 1:        $PRIVATE_1 (AZ: $AZ_1)"
          echo "  Private 2:        $PRIVATE_2 (AZ: $AZ_2)"
          echo "  Public 1:         ${PUBLIC_1:-<none>}"
          echo "  Public 2:         ${PUBLIC_2:-<none>}"
          
          echo "vpc_id=$VPC_ID" >> $GITHUB_OUTPUT
          echo "private_subnet_1=$PRIVATE_1" >> $GITHUB_OUTPUT
          echo "private_subnet_2=$PRIVATE_2" >> $GITHUB_OUTPUT
          echo "public_subnet_1=$PUBLIC_1" >> $GITHUB_OUTPUT
          echo "public_subnet_2=$PUBLIC_2" >> $GITHUB_OUTPUT

  cleanup-and-deploy:
    name: 🏗️ Deploy Infrastructure
    runs-on: ubuntu-latest
    needs: discover-infrastructure 
    environment: production
    outputs:
      api-endpoint: ${{ steps.export-outputs.outputs.api_endpoint }}
      schema-function-name: ${{ steps.export-outputs.outputs.schema_function_name }}
    steps:
      - uses: actions/checkout@v4
      
      - uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_DEFAULT_REGION }}
      
      - uses: actions/setup-python@v4
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          
      - name: Install Tools
        run: pip install aws-sam-cli boto3 jq

      - name: 🔥 Execute Comprehensive Cleanup
        id: cleanup
        timeout-minutes: 25
        continue-on-error: false
        run: |
          chmod +x scripts/cleanup.sh
          
          export STACK_NAME="${{ env.STACK_NAME }}"
          export AWS_DEFAULT_REGION="${{ env.AWS_DEFAULT_REGION }}"
          export RDS_DELETE_TIMEOUT_MINUTES="${{ env.RDS_DELETE_TIMEOUT_MINUTES }}"
          export STACK_DELETE_TIMEOUT_MINUTES="${{ env.STACK_DELETE_TIMEOUT_MINUTES }}"
          export MAX_RETRY_ATTEMPTS="${{ env.MAX_RETRY_ATTEMPTS }}"
          export CLEANUP_TIMEOUT_MINUTES="${{ env.CLEANUP_TIMEOUT_MINUTES }}"
          
          ./scripts/cleanup.sh

      - name: 🔨 Build PyMySQL Lambda Layer
        id: build-layer
        run: |
          set -euo pipefail
          
          echo "🔨 Building PyMySQL Lambda Layer..."
          cd aws
          
          mkdir -p layers/pymysql/python
          
          if [[ ! -f layers/pymysql/requirements.txt ]]; then
            echo "📝 Creating requirements.txt..."
            cat > layers/pymysql/requirements.txt << 'EOF'
          pymysql==1.1.0
          cryptography>=3.4.8
          EOF
          fi
          
          echo "📦 Installing dependencies for Lambda layer..."
          
          pip install \
            -r layers/pymysql/requirements.txt \
            -t layers/pymysql/python \
            --upgrade \
            --no-cache-dir \
            --platform manylinux2014_x86_64 \
            --only-binary=:all: \
            --python-version 3.11
          
          echo "🧹 Cleaning up unnecessary files..."
          find layers/pymysql/python -type d -name "__pycache__" -exec rm -rf {} + 2>/dev/null || true
          find layers/pymysql/python -type f -name "*.pyc" -delete 2>/dev/null || true
          find layers/pymysql/python -type d -name "*.dist-info" -exec rm -rf {} + 2>/dev/null || true
          find layers/pymysql/python -type d -name "tests" -exec rm -rf {} + 2>/dev/null || true
          
          if [ -d "layers/pymysql/python/pymysql" ]; then
            echo "✅ PyMySQL layer built successfully"
            echo "📊 Layer size: $(du -sh layers/pymysql/python | cut -f1)"
            echo "📋 PyMySQL version: $(python -c "import sys; sys.path.insert(0, 'layers/pymysql/python'); import pymysql; print(pymysql.__version__)")"
          else
            echo "❌ CRITICAL: PyMySQL not found in layer"
            echo "Layer contents:"
            ls -la layers/pymysql/python/ || true
            exit 1
          fi
          
          cd ..
          
          echo "✅ Layer preparation complete"

      - name: 🚀 Deploy SAM Application
        id: sam-deploy
        run: |
          set -euo pipefail
          cd aws
          
          ACCOUNT_ID=$(aws sts get-caller-identity --query Account --output text)
          BUCKET="sam-deployment-${ACCOUNT_ID}-${{ env.AWS_DEFAULT_REGION }}"
          
          echo "📦 Preparing S3 deployment bucket: $BUCKET"
          
          if ! aws s3api head-bucket --bucket "$BUCKET" 2>/dev/null; then
            echo "🆕 Creating deployment bucket..."
            if [[ "${{ env.AWS_DEFAULT_REGION }}" == "us-east-1" ]]; then
              aws s3api create-bucket --bucket "$BUCKET"
            else
              aws s3api create-bucket \
                --bucket "$BUCKET" \
                --create-bucket-configuration LocationConstraint="${{ env.AWS_DEFAULT_REGION }}"
            fi
            
            aws s3api put-bucket-versioning \
              --bucket "$BUCKET" \
              --versioning-configuration Status=Enabled
            
            echo "✅ Deployment bucket created"
          else
            echo "✅ Deployment bucket exists"
          fi
          
          JWT_SECRET=$(openssl rand -base64 48)
          
          VPC_ID="${{ needs.discover-infrastructure.outputs.vpc-id }}"
          PRIVATE_1="${{ needs.discover-infrastructure.outputs.private-subnet-1 }}"
          PRIVATE_2="${{ needs.discover-infrastructure.outputs.private-subnet-2 }}"
          PUBLIC_1="${{ needs.discover-infrastructure.outputs.public-subnet-1 }}"
          PUBLIC_2="${{ needs.discover-infrastructure.outputs.public-subnet-2 }}"
          
          echo ""
          echo "🔧 Deployment Configuration:"
          echo "  VPC:        $VPC_ID"
          echo "  Private 1:  $PRIVATE_1"
          echo "  Private 2:  $PRIVATE_2"
          echo "  Public 1:   ${PUBLIC_1:-<none>}"
          echo "  Public 2:   ${PUBLIC_2:-<none>}"
          
          PARAMS=(
            "Stage=prod"
            "JwtSecret=$JWT_SECRET"
            "CorsOrigins=https://yourdomain.com"
            "VpcId=$VPC_ID"
            "PrivateSubnetId1=$PRIVATE_1"
            "PrivateSubnetId2=$PRIVATE_2"
          )
          
          if [[ -n "$PUBLIC_1" ]]; then
            PARAMS+=("PublicSubnetId1=$PUBLIC_1")
          fi
          
          if [[ -n "$PUBLIC_2" ]]; then
            PARAMS+=("PublicSubnetId2=$PUBLIC_2")
          fi
          
          echo ""
          echo "📦 Deploying stack with ${#PARAMS[@]} parameters..."
          
          MAX_DEPLOY_RETRIES=3
          DEPLOY_ATTEMPT=1
          
          while [ $DEPLOY_ATTEMPT -le $MAX_DEPLOY_RETRIES ]; do
            echo "🚀 Deployment attempt $DEPLOY_ATTEMPT/$MAX_DEPLOY_RETRIES..."
            
            if sam deploy \
              --stack-name "${{ env.STACK_NAME }}" \
              --region ${{ env.AWS_DEFAULT_REGION }} \
              --s3-bucket "$BUCKET" \
              --parameter-overrides "${PARAMS[@]}" \
              --capabilities CAPABILITY_IAM CAPABILITY_NAMED_IAM \
              --no-fail-on-empty-changeset \
              --no-confirm-changeset \
              --on-failure ROLLBACK 2>&1 | tee deploy.log; then
              
              echo "✅ SAM deployment successful"
              echo "deploy_status=success" >> $GITHUB_OUTPUT
              break
            else
              echo "❌ Deployment attempt $DEPLOY_ATTEMPT failed"
              
              if [ $DEPLOY_ATTEMPT -lt $MAX_DEPLOY_RETRIES ]; then
                echo "⏳ Waiting 30s before retry..."
                sleep 30
              else
                echo "❌ All deployment attempts failed"
                echo "deploy_status=failed" >> $GITHUB_OUTPUT
                cat deploy.log || true
                exit 1
              fi
            fi
            
            DEPLOY_ATTEMPT=$((DEPLOY_ATTEMPT + 1))
          done

      - name: 📤 Export Stack Outputs
        id: export-outputs
        run: |
          set -euo pipefail
          STACK="${{ env.STACK_NAME }}"
          
          echo "📤 Fetching CloudFormation outputs..."
          
          for i in {1..30}; do
            STATUS=$(aws cloudformation describe-stacks \
              --stack-name "$STACK" \
              --query 'Stacks[0].StackStatus' \
              --output text 2>/dev/null || echo "NOT_FOUND")
            
            if [[ "$STATUS" =~ (CREATE_COMPLETE|UPDATE_COMPLETE) ]]; then
              echo "✅ Stack is ready: $STATUS"
              break
            elif [[ "$STATUS" =~ (FAILED|ROLLBACK) ]]; then
              echo "❌ Stack in error state: $STATUS"
              exit 1
            fi
            
            echo "⏳ Waiting for stack to stabilize... ($i/30)"
            sleep 10
          done
          
          API_ENDPOINT=$(aws cloudformation describe-stacks \
            --stack-name "$STACK" \
            --query 'Stacks[0].Outputs[?OutputKey==`ApiEndpoint`].OutputValue' \
            --output text)
          
          SCHEMA_FUNCTION_NAME=$(aws cloudformation describe-stacks \
            --stack-name "$STACK" \
            --query 'Stacks[0].Outputs[?OutputKey==`SchemaInitializerFunctionName`].OutputValue' \
            --output text)
          
          if [[ -z "$API_ENDPOINT" || "$API_ENDPOINT" == "None" ]]; then
            echo "❌ API endpoint not found in outputs"
            exit 1
          fi
          
          if [[ -z "$SCHEMA_FUNCTION_NAME" || "$SCHEMA_FUNCTION_NAME" == "None" ]]; then
            echo "❌ Schema function name not found in outputs"
            exit 1
          fi
          
          echo "api_endpoint=$API_ENDPOINT" >> $GITHUB_OUTPUT
          echo "schema_function_name=$SCHEMA_FUNCTION_NAME" >> $GITHUB_OUTPUT
          
          echo "✅ Outputs exported:"
          echo "  API Endpoint: $API_ENDPOINT"
          echo "  Schema Function: $SCHEMA_FUNCTION_NAME"

  initialize-database:
    name: 🗃️ Initialize Database Schema
    runs-on: ubuntu-latest
    needs: cleanup-and-deploy
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      
      - uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_DEFAULT_REGION }}
      
      - uses: actions/setup-python@v4
        with:
          python-version: ${{ env.PYTHON_VERSION }}
      
      - name: Install dependencies
        run: pip install boto3
      
      - name: 🗃️ Parse SQL Schema File
        id: parse-sql
        run: |
          set -euo pipefail
          
          if [ -f database/rds_schema_update.sql ]; then
            echo "📄 Parsing SQL schema file..."
            
            python3 <<'PYEOF'
          import json
          import sys
          
          try:
              with open('database/rds_schema_update.sql', 'r', encoding='utf-8') as f:
                  sql = f.read()
              
              statements = [
                  s.strip() 
                  for s in sql.split(';') 
                  if s.strip() and not s.strip().startswith('--') and len(s.strip()) > 5
              ]
              
              with open('sql_statements.json', 'w') as out:
                  json.dump(statements, out, indent=2)
              
              print(f"✅ Successfully parsed {len(statements)} SQL statements")
              sys.exit(0)
              
          except Exception as e:
              print(f"❌ Error parsing SQL file: {str(e)}")
              sys.exit(1)
          PYEOF
            
          else
            echo "⚠️ No SQL schema file found at database/rds_schema_update.sql"
            echo "Using default empty schema"
            echo '[]' > sql_statements.json
          fi
      
      - name: 🚀 Invoke Schema Initializer Lambda
        timeout-minutes: 8
        run: |
          set -euo pipefail
          STACK="${{ env.STACK_NAME }}"
          
          echo "🔍 Locating schema initializer Lambda..."
          
          FUNCTION_NAME="${{ needs.cleanup-and-deploy.outputs.schema-function-name }}"
          
          if [[ -z "$FUNCTION_NAME" || "$FUNCTION_NAME" == "None" ]]; then
            echo "❌ Schema initializer Lambda function not found"
            echo "Available stack outputs:"
            aws cloudformation describe-stacks \
              --stack-name "$STACK" \
              --query 'Stacks[0].Outputs[].[OutputKey,OutputValue]' \
              --output table
            exit 1
          fi
          
          echo "✅ Found function: $FUNCTION_NAME"
          
          FUNCTION_STATE=$(aws lambda get-function \
            --function-name "$FUNCTION_NAME" \
            --query 'Configuration.State' \
            --output text 2>/dev/null || echo "NOT_FOUND")
          
          if [[ "$FUNCTION_STATE" != "Active" ]]; then
            echo "⚠️ Lambda function state: $FUNCTION_STATE"
            echo "Waiting for function to become active..."
            
            for i in {1..30}; do
              FUNCTION_STATE=$(aws lambda get-function \
                --function-name "$FUNCTION_NAME" \
                --query 'Configuration.State' \
                --output text 2>/dev/null || echo "NOT_FOUND")
              
              if [[ "$FUNCTION_STATE" == "Active" ]]; then
                echo "✅ Function is now active"
                break
              fi
              
              echo "⏳ Waiting... ($i/30)"
              sleep 10
            done
          fi
          
          SQL_STATEMENTS=$(cat sql_statements.json)
          STMT_COUNT=$(echo "$SQL_STATEMENTS" | jq 'length')
          
          echo "📝 Prepared payload with $STMT_COUNT SQL statements"
          
          PAYLOAD=$(jq -n --argjson stmts "$SQL_STATEMENTS" '{sql_statements: $stmts}')
          
          MAX_RETRIES=3
          RETRY_COUNT=0
          
          while [ $RETRY_COUNT -lt $MAX_RETRIES ]; do
            echo "📤 Invoking schema initializer (attempt $((RETRY_COUNT + 1))/$MAX_RETRIES)..."
            
            if aws lambda invoke \
              --function-name "$FUNCTION_NAME" \
              --payload "$PAYLOAD" \
              --cli-binary-format raw-in-base64-out \
              --log-type Tail \
              response.json 2>&1; then
              
              echo "✅ Lambda invocation successful"
              break
            else
              RETRY_COUNT=$((RETRY_COUNT + 1))
              if [ $RETRY_COUNT -lt $MAX_RETRIES ]; then
                echo "⏳ Retrying in 10 seconds..."
                sleep 10
              else
                echo "❌ Lambda invocation failed after $MAX_RETRIES attempts"
                exit 1
              fi
            fi
          done
          
          echo ""
          echo "📥 Lambda Response:"
          cat response.json | jq '.' || cat response.json
          
          if jq -e '.errorMessage' response.json >/dev/null 2>&1; then
            echo ""
            echo "❌ Lambda execution error:"
            jq -r '.errorMessage' response.json
            exit 1
          fi
          
          BODY=$(jq -r '.body' response.json 2>/dev/null || echo "null")
          
          if [[ "$BODY" == "null" || -z "$BODY" ]]; then
            echo "❌ No response body returned from Lambda"
            exit 1
          fi
          
          SUCCESS=$(echo "$BODY" | jq -r '.success // false')
          ERRORS=$(echo "$BODY" | jq -r '.summary.errors // 0')
          EXECUTED=$(echo "$BODY" | jq -r '.summary.executed // 0')
          SKIPPED=$(echo "$BODY" | jq -r '.summary.skipped // 0')
          TOTAL=$(echo "$BODY" | jq -r '.summary.total // 0')
          
          echo ""
          echo "📊 Schema Initialization Summary:"
          echo "  ✅ Executed: $EXECUTED"
          echo "  ⏭️  Skipped:  $SKIPPED (already exists)"
          echo "  ❌ Errors:   $ERRORS"
          echo "  📊 Total:    $TOTAL"
          
          if [[ "$SUCCESS" == "true" ]]; then
            echo ""
            echo "✅ Schema initialization completed successfully!"
            exit 0
          elif [[ "$ERRORS" -gt 0 && "$EXECUTED" -gt 0 ]]; then
            echo ""
            echo "✅ Schema initialization completed (with non-fatal 'already exists' errors)."
            exit 0
          elif [[ "$ERRORS" -gt 0 && "$EXECUTED" -eq 0 && "$SKIPPED" -gt 0 ]]; then
            echo ""
            echo "✅ Schema initialization complete (all items skipped or already exist)."
            exit 0
          else
            echo ""
            echo "❌ Critical schema initialization failure"
            echo ""
            echo "Detailed errors:"
            echo "$BODY" | jq '.results[] | select(.status == "error")'
            exit 1
          fi

  deploy-frontend:
    name: 🌐 Deploy Frontend - Conservative Stack
    runs-on: ubuntu-latest
    needs: [cleanup-and-deploy, initialize-database]
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      
      - uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_DEFAULT_REGION }}
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
      
      - name: 🧹 Complete Cleanup
        working-directory: ./frontend
        run: |
          set -euo pipefail
          
          echo "🧹 Performing complete cleanup..."
          
          rm -rf node_modules build .npm package-lock.json .env.production
          npm cache clean --force
          
          echo "✅ Cleanup complete"
      
      - name: 📦 Install Dependencies with Conservative Stack
        working-directory: ./frontend
        timeout-minutes: 10
        run: |
          set -euo pipefail
          
          echo "📦 Installing dependencies..."
          
          cat > .npmrc << 'EOF'
          legacy-peer-deps=true
          strict-peer-dependencies=false
          auto-install-peers=true
          EOF
          
          npm install --legacy-peer-deps
          
          echo ""
          echo "🔧 Force installing exact compatible versions for react-scripts@5.0.1..."
          
          npm install --save-dev --legacy-peer-deps --force \
            schema-utils@2.7.1 \
            ajv@6.12.6 \
            ajv-keywords@3.5.2 \
            fork-ts-checker-webpack-plugin@6.5.3
          
          npm dedupe --legacy-peer-deps || true
          
          echo "✅ Dependencies installed"
      
      - name: 🔍 Verify Compatibility
        working-directory: ./frontend
        run: |
          set -euo pipefail
          
          echo "🔍 Verifying package versions..."
          
          node -e "
            const fs = require('fs');
            
            const packages = {
              'schema-utils': 'node_modules/schema-utils/package.json',
              'ajv': 'node_modules/ajv/package.json',
              'fork-ts-checker-webpack-plugin': 'node_modules/fork-ts-checker-webpack-plugin/package.json'
            };
            
            console.log('📋 Installed Versions:');
            const versions = {};
            
            Object.entries(packages).forEach(([name, path]) => {
              try {
                const pkg = JSON.parse(fs.readFileSync(path, 'utf8'));
                versions[name] = pkg.version;
                console.log(\`  ✅ \${name.padEnd(40)} v\${pkg.version}\`);
              } catch (err) {
                console.log(\`  ❌ \${name.padEnd(40)} NOT FOUND\`);
                process.exit(1);
              }
            });
            
            console.log('');
            
            const schemaUtilsMajor = parseInt(versions['schema-utils'].split('.')[0]);
            const forkTsCheckerMajor = parseInt(versions['fork-ts-checker-webpack-plugin'].split('.')[0]);
            
            if (schemaUtilsMajor === 2 && forkTsCheckerMajor === 6) {
              console.log('✅ Compatible: schema-utils@2.x + fork-ts-checker@6.x');
              console.log('✅ This combination works with react-scripts@5.0.1');
            } else {
              console.log('❌ Incompatible versions detected');
              process.exit(1);
            }
            
            const schemaUtils = require('schema-utils');
            if (typeof schemaUtils === 'function' || typeof schemaUtils.validate === 'function') {
              console.log('✅ schema-utils validation function available');
            } else {
              console.log('❌ schema-utils validation not available');
              process.exit(1);
            }
          "
      - name: 🏗️ Build React Application
        working-directory: ./frontend
        timeout-minutes: 10
        env:
          REACT_APP_API_URL: ${{ needs.cleanup-and-deploy.outputs.api-endpoint }}
          REACT_APP_STAGE: prod
          REACT_APP_VERSION: ${{ github.sha }}
          CI: false
          NODE_OPTIONS: "--max_old_space_size=4096"
          SKIP_PREFLIGHT_CHECK: true
        run: |
          set -euo pipefail
          
          echo "🏗️ ============================================"
          echo "🏗️  BUILDING REACT APPLICATION"
          echo "🏗️ ============================================"
          echo ""
          
          API_ENDPOINT="${{ needs.cleanup-and-deploy.outputs.api-endpoint }}"
          
          echo "✅ API Endpoint: $API_ENDPOINT"
          
          cat > .env.production << EOF
          REACT_APP_API_URL=$API_ENDPOINT
          REACT_APP_STAGE=prod
          REACT_APP_VERSION=$(date +%Y%m%d-%H%M%S)
          REACT_APP_BUILD_TIME=$(date -u '+%Y-%m-%d %H:%M:%S UTC')
          EOF
          
          echo "📝 Environment:"
          cat .env.production
          echo ""
          
          echo "🔨 Building..."
          BUILD_START=$(date +%s)
          
          if npm run build 2>&1 | tee build.log; then
            BUILD_END=$(date +%s)
            BUILD_DURATION=$((BUILD_END - BUILD_START))
            
            echo ""
            echo "✅ ============================================"
            echo "✅  BUILD COMPLETED IN ${BUILD_DURATION}s"
            echo "✅ ============================================"
          else
            echo ""
            echo "❌ Build failed:"
            tail -50 build.log
            exit 1
          fi
          
          if [[ ! -f build/index.html ]]; then
            echo "❌ Build output missing"
            exit 1
          fi
          
          echo ""
          echo "📊 Build size: $(du -sh build | cut -f1)"
          echo "✅ Build validation complete"
      
      
      - name: 🚀 Deploy to S3
        timeout-minutes: 10
        run: |
          set -euo pipefail
          FRONTEND_BUCKET="${{ env.STACK_NAME }}-frontend"
          
          echo "🚀 ============================================"
          echo "🚀  S3 FRONTEND DEPLOYMENT"
          echo "🚀 ============================================"
          echo ""
          
          echo "🪣 Target bucket: $FRONTEND_BUCKET"
          echo "📍 Region: ${{ env.AWS_DEFAULT_REGION }}"
          echo ""
          
          echo "🔍 Checking if bucket exists..."
          
          if aws s3api head-bucket --bucket "$FRONTEND_BUCKET" 2>/dev/null; then
            echo "✅ Bucket exists: $FRONTEND_BUCKET"
          else
            echo "🆕 Creating S3 bucket: $FRONTEND_BUCKET"
            
            if [[ "${{ env.AWS_DEFAULT_REGION }}" == "us-east-1" ]]; then
              aws s3api create-bucket --bucket "$FRONTEND_BUCKET"
            else
              aws s3api create-bucket \
                --bucket "$FRONTEND_BUCKET" \
                --create-bucket-configuration LocationConstraint="${{ env.AWS_DEFAULT_REGION }}"
            fi
            
            sleep 5
          fi
          
          echo ""
          echo "📤 Uploading static assets..."
          
          aws s3 sync frontend/build/ "s3://$FRONTEND_BUCKET" \
            --delete \
            --cache-control "public,max-age=31536000,immutable" \
            --exclude "*.html" \
            --exclude "service-worker.js" \
            --exclude "asset-manifest.json" \
            --exclude "manifest.json" \
            --exclude "robots.txt"
          
          echo ""
          echo "📤 Uploading HTML files..."
          
          aws s3 sync frontend/build/ "s3://$FRONTEND_BUCKET" \
            --exclude "*" \
            --include "*.html" \
            --include "service-worker.js" \
            --include "asset-manifest.json" \
            --include "manifest.json" \
            --include "robots.txt" \
            --cache-control "no-cache,no-store,must-revalidate" \
            --metadata-directive REPLACE
          
          echo ""
          echo "🌐 Configuring static website hosting..."
          
          aws s3api put-bucket-website \
            --bucket "$FRONTEND_BUCKET" \
            --website-configuration '{
              "IndexDocument": {"Suffix": "index.html"},
              "ErrorDocument": {"Key": "index.html"}
            }'
          
          echo ""
          echo "🔓 Configuring public access..."
          
          aws s3api put-public-access-block \
            --bucket "$FRONTEND_BUCKET" \
            --public-access-block-configuration \
            "BlockPublicAcls=false,IgnorePublicAcls=false,BlockPublicPolicy=false,RestrictPublicBuckets=false"
          
          aws s3api put-bucket-policy \
            --bucket "$FRONTEND_BUCKET" \
            --policy '{
              "Version": "2012-10-17",
              "Statement": [{
                "Sid": "PublicReadGetObject",
                "Effect": "Allow",
                "Principal": "*",
                "Action": "s3:GetObject",
                "Resource": "arn:aws:s3:::'$FRONTEND_BUCKET'/*"
              }]
            }'
          
          WEBSITE_URL="http://$FRONTEND_BUCKET.s3-website-${{ env.AWS_DEFAULT_REGION }}.amazonaws.com"
          
          echo ""
          echo "✅ ============================================"
          echo "✅  FRONTEND DEPLOYMENT COMPLETE"
          echo "✅ ============================================"
          echo ""
          echo "🌐 Website URL: $WEBSITE_URL"
          echo ""

      - name: 🧪 Post-Deployment Validation
        timeout-minutes: 5
        run: |
          set -euo pipefail
          
          WEBSITE_URL="http://${{ env.STACK_NAME }}-frontend.s3-website-${{ env.AWS_DEFAULT_REGION }}.amazonaws.com"
          
          echo "🧪 Testing frontend accessibility..."
          echo "URL: $WEBSITE_URL"
          echo ""
          
          MAX_RETRIES=12
          RETRY_COUNT=0
          
          while [ $RETRY_COUNT -lt $MAX_RETRIES ]; do
            if curl -sf -o /tmp/index.html "$WEBSITE_URL" 2>/dev/null; then
              echo "✅ Frontend is accessible!"
              
              if grep -q "<!DOCTYPE html>" /tmp/index.html || grep -q "<html" /tmp/index.html; then
                echo "✅ Valid HTML content detected"
                
                if grep -q "root" /tmp/index.html; then
                  echo "✅ React root element found"
                fi
                
                break
              else
                echo "⚠️ Response doesn't appear to be HTML"
              fi
            fi
            
            RETRY_COUNT=$((RETRY_COUNT + 1))
            
            if [ $RETRY_COUNT -lt $MAX_RETRIES ]; then
              echo "⏳ Retry $RETRY_COUNT/$MAX_RETRIES - waiting 10s..."
              sleep 10
            else
              echo "⚠️ Frontend validation timeout (non-fatal)"
              echo "Website may still be propagating - check manually:"
              echo "  $WEBSITE_URL"
            fi
          done
          
          echo ""
          echo "✅ Post-deployment validation complete"

  smoke-tests:
    name: 🧪 Smoke Tests & Validation
    runs-on: ubuntu-latest
    needs: [cleanup-and-deploy, initialize-database, deploy-frontend]
    timeout-minutes: 10
    steps:
      - uses: actions/setup-python@v4
        with:
          python-version: ${{ env.PYTHON_VERSION }}
      
      - name: 🧪 Comprehensive Smoke Tests
        env:
          API_ENDPOINT: ${{ needs.cleanup-and-deploy.outputs.api-endpoint }}
        run: |
          set -euo pipefail
          
          echo "🧪 Starting comprehensive smoke tests"
          echo "API Endpoint: $API_ENDPOINT"
          echo "======================================"
          
          echo ""
          echo "🏥 Test 1: API Health Check"
          
          for i in {1..24}; do
            if curl -sf "${API_ENDPOINT}/health" -o health_response.txt 2>/dev/null; then
              echo "✅ Health check passed"
              python3 -m json.tool < health_response.txt
              break
            else
              if [ $i -lt 24 ]; then
                echo "⏳ Retry $i/24..."
                sleep 10
              else
                echo "⚠️ Health check timeout (non-fatal)"
              fi
            fi
          done
          
          echo ""
          echo "🌐 Test 2: Frontend Website"
          FRONTEND_URL="http://${{ env.STACK_NAME }}-frontend.s3-website-${{ env.AWS_DEFAULT_REGION }}.amazonaws.com"
          
          if curl -sf "$FRONTEND_URL" >/dev/null 2>&1; then
            echo "✅ Frontend is accessible at: $FRONTEND_URL"
          else
            echo "⚠️ Frontend may still be propagating"
          fi
          
          echo ""
          echo "✅ Smoke tests completed"

  deployment-summary:
    name: 📊 Deployment Summary
    runs-on: ubuntu-latest
    needs: [cleanup-and-deploy, discover-infrastructure, initialize-database, deploy-frontend, smoke-tests]
    if: always()
    steps:
      - name: 🎉 Generate Deployment Report
        run: |
          echo "🎆 ============================================================"
          echo "🎆  PRODUCTION DEPLOYMENT COMPLETE"
          echo "🎆 ============================================================"
          echo ""
          echo "📊 Deployment Summary:"
          echo "  Stack Name:          ${{ env.STACK_NAME }}"
          echo "  Region:              ${{ env.AWS_DEFAULT_REGION }}"
          echo "  Timestamp:           $(date -u '+%Y-%m-%d %H:%M:%S UTC')"
          echo ""
          echo "🏗️ Infrastructure:"
          echo "  VPC ID:              ${{ needs.discover-infrastructure.outputs.vpc-id }}"
          echo "  Private Subnet 1:    ${{ needs.discover-infrastructure.outputs.private-subnet-1 }}"
          echo "  Private Subnet 2:    ${{ needs.discover-infrastructure.outputs.private-subnet-2 }}"
          echo ""
          echo "🔗 Endpoints:"
          echo "  API Endpoint:        ${{ needs.cleanup-and-deploy.outputs.api-endpoint }}"
          echo "  Frontend:            http://${{ env.STACK_NAME }}-frontend.s3-website-${{ env.AWS_DEFAULT_REGION }}.amazonaws.com"
          echo ""
          echo "🎯 Deployment Status:"
          
          CLEANUP_STATUS="${{ needs.cleanup-and-deploy.result }}"
          DB_STATUS="${{ needs.initialize-database.result }}"
          FRONTEND_STATUS="${{ needs.deploy-frontend.result }}"
          SMOKE_STATUS="${{ needs.smoke-tests.result }}"
          
          echo "  Cleanup & Deploy:    $CLEANUP_STATUS"
          echo "  Database Init:       $DB_STATUS"
          echo "  Frontend Deploy:     $FRONTEND_STATUS"
          echo "  Smoke Tests:         $SMOKE_STATUS"
          echo ""
          
          if [[ "$CLEANUP_STATUS" == "success" ]] && \
             [[ "$DB_STATUS" == "success" ]] && \
             [[ "$FRONTEND_STATUS" == "success" ]]; then
            echo "🎉 ============================================================"
            echo "🎉  DEPLOYMENT SUCCESSFUL - ALL SYSTEMS OPERATIONAL"
            echo "🎉 ============================================================"
            exit 0
          else
            echo "❌ ============================================================"
            echo "❌  DEPLOYMENT FAILED - REVIEW LOGS"
            echo "❌ ============================================================"
            exit 1
          fi